# --- required ---
BOT_TOKEN=8080027534:AAFaKuXRDkfVWlM4osuyXPXAOnzF2hQZd94
ADMIN_IDS=7940894807           # comma-separated Telegram user IDs allowed to configure

# --- polling speed (seconds) ---
INTERVAL_NORMAL=10
INTERVAL_FAST=3
INTERVAL_INSANE=1

# --- limits & rules (0 = no limit) ---
DEFAULT_MAX_PRICE=0            # price cap in Stars (0 = unlimited)
DEFAULT_ALLOW_IDS=             # comma-separated Gift IDs (leave blank = any)
DEFAULT_ALLOW_KEYS=teddy,ring  # comma-separated keywords to match titles
DEFAULT_RECIPS=123456789       # comma-separated recipients; first one is used
BUY_COOLDOWN_SEC=600           # per-gift cooldown in seconds
DEFAULT_WINDOW=                # "HH:MM-HH:MM" local time window; empty = always
DEFAULT_NOTIFY_ONLY=false      # true = alert instead of buying when over cap

# --- source: Bot API or external notifier JSON feed ---
FEED_MODE=api                  # "api" or "notifier"
NOTIFIER_JSON=/tmp/gifts.json  # path written by tg_gifts_notifier (if FEED_MODE=notifier)
NOTIFIER_POLL_SEC=1.0          # how often to read the JSON (seconds)
